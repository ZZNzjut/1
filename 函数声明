#include<iostream>
using namespace std;

int max(int a, int b);//定义在main（）的return 0;后时用声明
int max(int a, int b);//声明可以多次，但定义只能一次


int main() {

	int a = 10;
	int b = 20;
	cout << max(a, b) << endl;







	system("pause");
	return 0;
}
int max(int a, int b)
{
	return a > b ? a : b;
}
